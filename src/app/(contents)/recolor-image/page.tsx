'use client'

import React from 'react'

const Page = () => {
  const handleClick = async () => {
    
  }

  return (
    <div>
      Recolor image
      <div className="mt-5"><button className="btn btn-primary" onClick={handleClick}>Click</button></div>
    </div>
  )
}

export default Page
